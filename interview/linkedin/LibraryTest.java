package interview.linkedin;
/*
import static org.junit.Assert.*;
import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;

public class LibraryTest {

	double delta = 0.001;
	Library l;
	
	@BeforeClass
	public static void setUpBeforeClass() throws Exception {
		System.out.println("setUpBeforeClass");
	}

	@AfterClass
	public static void tearDownAfterClass() throws Exception {
		System.out.println("tearDownAfterClass");
	}

	@Before
	public void setUp() throws Exception {
		System.out.println("setUp");
	}

	@After
	public void tearDown() throws Exception {
		System.out.println("tearDown");
	}

	@Test
	public void testDepthSum() {
		assertEquals(1, 1, delta);
	}

	@Test
	public void testMyPow() {
		assertEquals(1, 1, delta);
	}

	@Test
	public void testMyPow2() {
		assertEquals(1, 1, delta);
	}

	@Test
	public void testIsNumber() {
		l = new Library();
		assertTrue(l.isNumber("0"));
		assertFalse(l.isNumber("a"));
		assertFalse(l.isNumber("-10."));
		assertTrue(l.isNumber("-10.100"));
		assertTrue(l.isNumber("+0010.1230"));
		assertFalse(l.isNumber("--10.00"));
		assertFalse(l.isNumber("-10.0.0.1"));
	}
	@Test
	public void testCountOverlap() {
		l = new Library();
		assertEquals(l.countOverlap(new int[][] {
				{1,4}, {-2,3}, {9,10}
		}), 7, delta);
		assertEquals(l.countOverlap(new int[][] {
				{-6, -4}, {-8, -6}, {-6, -5}, {-6, 8}
		}), 16, delta);
	}

}
*/